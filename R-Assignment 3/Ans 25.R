#25. Write R code that dynamically adds named elements to an existing list based on user input. Start with an empty list

my_list <- list()

my_list[["name"]] <- "John"
my_list[["age"]] <- "30"
my_list[["city"]] <- "Paris"

my_list
